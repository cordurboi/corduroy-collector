# Public web app environment variables (safe for client-side)

# URL of the deployed API (Railway, Render, etc.)
NEXT_PUBLIC_API_URL=https://your-api-host.example.com

# Web3Auth client ID (public, but keep real value out of examples)
NEXT_PUBLIC_WEB3AUTH_CLIENT_ID=YOUR_WEB3AUTH_CLIENT_ID

# RPC endpoint for Moonbase Alpha
NEXT_PUBLIC_RPC_URL=https://rpc.api.moonbase.moonbeam.network
